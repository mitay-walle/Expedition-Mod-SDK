name: Repository audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject private binaries and non-Unity package sources
        shell: pwsh
        run: ./Tools/audit_repository.ps1
